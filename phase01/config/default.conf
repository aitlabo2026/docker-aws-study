server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;
    add_header X-Learning-Phase "phase01" always;
    location / {
        try_files $uri $uri/ =404;
    }
}
