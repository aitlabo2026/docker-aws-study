# Notes

- An image is the read-only template; a container is its runtime instance.
- A named volume keeps data independently from a container lifecycle.
- A user-defined network provides container-name DNS resolution.
- Runtime environment variables can change without rebuilding the image.
- Secrets must not be stored in source files, images, or Git.
